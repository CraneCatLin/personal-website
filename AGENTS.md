## Cline project context

在开始任何实质性工作、分析问题或修改代码之前，必须先检查 `.clinerules/` 目录。

`.clinerules/` 不仅包含开发规则，还包含已经整理好的：

* 项目整体结构
* 模块划分
* 架构说明
* 关键组件职责
* 开发约定
* 历史设计背景
* 与特定模块相关的说明和规则

因此，不得因为当前使用的 Agent 不是 Cline 而忽略该目录。

开始任务时：

1. 先检查 `.clinerules/` 目录的内容和文件组织。
2. 阅读其中描述项目整体结构、架构和模块关系的相关文件，以建立对项目的基本理解。
3. 阅读与当前任务、目标文件、相关模块或所涉及功能有关的规则和说明文件。
4. 在修改代码前，确保已经理解相关模块在整个项目中的位置、职责以及已有设计约束。
5. `.clinerules/` 中与当前任务相关的规则，应视为项目级开发规范并遵守。

不要求机械地执行 `.clinerules/` 中所有与当前任务无关的规则。应自行判断哪些内容属于：

* 当前任务必须遵守的规则；
* 有助于理解项目的背景信息；
* 与当前任务无关、可以暂时忽略的内容。

如果 `.clinerules/` 中的内容与本 `AGENTS.md` 或用户当前的直接指令发生冲突，优先级为：

用户当前直接指令 > `AGENTS.md` > `.clinerules/`

不得跳过对 `.clinerules/` 的检查，仅凭代码本身推测已经有明确文档说明的项目结构或设计意图。
